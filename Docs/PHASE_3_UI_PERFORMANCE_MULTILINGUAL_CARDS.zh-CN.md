# 第三阶段：训练家终端 UI、顺畅度与中英日卡牌

状态：软件范围已完成并通过 100% 验收（2026-07-30）。

## 一、目标

第三阶段在第二阶段已验收的完整图鉴与英文卡牌资料之上完成三件事：

1. 将玩家界面统一为原创的“训练家终端 / 图鉴设备”视觉语言，使其具有鲜明的宝可梦冒险氛围，但不复制官方界面、Logo、角色素材或音频。
2. 优化中端 Android 上的列表滚动、页面切换、图片解码、搜索筛选和动画调度；正常操作不能因为全量资料增长而一次加载所有卡图。
3. 卡牌内容扩展为英文 `en`、日文 `ja`、简体中文 `zh-cn`，并把“界面语言”与“卡牌印刷语言”继续保持为两个独立选择。

应用语言与卡片语言是两个完全隔离的持久状态：两者使用不同存档键，不互相写入、不互相触发事件，首次启动也不根据应用语言猜测卡片语言。应用语言只控制菜单、按钮、提示和图鉴说明；卡片语言只控制系列、抽卡池、卡图与实体印刷资料。未安装卡牌目录时只能禁用卡片语言选择器，应用语言仍须保持可用。

## 二、已经确认的玩家规则

卡牌语言不是对同一张英文卡做文字翻译，而是独立的实体印刷版本。

- 一个卡牌版本组只有一种已安装语言时，直接显示该卡，不显示无效语言切换器。
- 同组存在两种或三种已安装语言时，详情页显示 `中 / EN / 日` 分段切换。
- 切换后必须一起更换卡图、卡名、所属系列、卡号、稀有度、变体与来源信息。
- 没有对应印刷版的语言不显示空白标签，不机翻、不伪造卡图，也不回退成另一语言后冒充目标语言。
- 全局“卡牌内容语言”决定抽卡、系列列表与默认详情版本；详情页的临时语言切换只影响当前版本组，不偷偷修改全局设置。
- 卡牌详情里的 `中 / EN / 日` 不修改应用语言，也不修改全局卡片语言。
- 切换详情语言后仍保持同一逻辑卡组；关闭详情并重新打开时，以全局内容语言优先，否则选择该组实际存在的第一个版本。

## 三、身份与匹配模型

现有 `PrintingDefinition` 继续代表一个确定语言、系列、卡号和变体的实体印刷版。新增独立的跨语言版本组，不破坏收藏数量与抽卡记录：

```text
CrossLanguageCardGroup
  groupId
  members[]
    languageId
    printingId
    sourceCardId
    setId / localId / variantId
  matchMethod
  confidence
  reviewStatus
```

匹配必须保守且可审计：

1. 优先使用来源提供的稳定跨语言身份或经过版本控制的系列映射。
2. 再使用系列、卡号、插画家、宝可梦主体、规则字段和卡图感知特征的组合证据。
3. 自动结果必须记录方法与置信度；有歧义的重印、合辑、不同插画和语言独占卡进入复核清单。
4. 低置信度项目保持为各自独立卡，不为了增加语言按钮而错误合并。
5. 收藏存档继续按 `printingId` 计数；跨语言组只负责浏览和跳转，不合并玩家拥有数量。

## 四、当前容量基线

以下数字来自 2026-07-30 revision 4 正式 Catalog 与最终 ARM64 APK，不是旧日志估计：

| 范围 | 当前容量 |
|---|---:|
| ARM64 APK | 52,643,378 bytes（50.20 MiB） |
| 英文 218 个 Set | 下载 350,550,171 bytes（334.31 MiB）；安装 365,127,653 bytes（348.21 MiB） |
| 日文 177 个 Set | 下载 68,951,299 bytes（65.76 MiB）；安装 74,252,344 bytes（70.81 MiB） |
| 简中 129 个 Set | 下载 684,086,596 bytes（652.40 MiB）；安装 692,064,874 bytes（660.00 MiB） |
| 图鉴、9 个世代图片与三语言卡牌关联 | 下载 198,305,688 bytes（189.12 MiB）；安装 224,821,304 bytes（214.41 MiB） |
| 正式远端 Catalog 全量资源 | 下载 1,301,893,754 bytes（1.212 GiB）；安装 1,356,266,175 bytes（1.263 GiB） |
| 手机 APK + 全量已安装资源 | 1,408,909,553 bytes（1.312 GiB） |

Site 当前 revision 4 Catalog 引用 537 个不可变 ZIP，总下载量为 1.212 GiB；旧版、未引用对象是否仍占远端配额，不能从公开只读 Catalog 精确得出，迁移 R2 前需要用管理端清单单独审计。

### 4.1 中英日最终收据

最终来源与运行时收据为：英文 218 Set / 23,444 来源卡，日文 177 Set / 8,159 来源卡，简中 129 Set / 12,473 来源卡；安装后共有 499 个逻辑 Set、43,705 张逻辑卡与 53,480 个实体印刷版本。简中来源本身提供的图片体积显著高于早期按英文平均值作出的估算，因此最终全装容量为 1.312 GiB，而不是旧预算的 0.92 GiB 左右。

完整发行还包含 1 个 taxonomy、3 个按卡牌语言拆分的物种/形态关联包和 9 个世代图鉴图片包。最大单包 27,914,532 bytes；537 个安装收据、44,076 个卡牌主体关联、1,571 张图鉴图片和全部 Hash 回读失败均为 0。

## 五、实施切片与验收

| 切片 | 工作 | 验收证据 | 状态 |
|---|---|---|---|
| 3A 基线与契约 | 记录容量、语言范围、跨语言行为和失败边界 | 本文、清单解析测试、工作区审计 | 已完成 |
| 3B 多语言领域层 | 新增版本组、可用语言解析、稳定默认选择与收藏隔离 | EditMode：1/2/3 语言、缺失语言、歧义不合并、存档不变 | 已完成 |
| 3C 私人导入与打包 | 导入 `ja`、`zh-cn` metadata/WebP；按语言/Set 打包、Hash、离线回读 | 每语言 Set/卡/图片/失败数量；双构建 Hash；包大小门槛 | 已完成 |
| 3D 卡牌语言切换 UI | 收藏详情与图鉴相关卡详情显示按实际可用语言生成的标签 | PlayMode：单语言隐藏、多语言切换全字段、无空标签；点击音效/翻卡动画/减少动态效果 | 已完成 |
| 3E 统一视觉升级 | 统一终端外壳、红蓝黄主色、胶囊导航、扫描线/状态灯、卡牌与图鉴层级 | 四个主要页面截图/结构测试；中文英文排版；小屏无溢出 | 已完成 |
| 3F 顺畅度优化 | 预索引、搜索防抖、虚拟化复用、图片请求限流/取消、共享缓存预算、低成本动画 | 性能测试、滚动/搜索 PlayMode、无持续增长、减少动态效果 | 已完成 |
| 3G 发布与最终验收 | 合并 Catalog 后上传，只读验证，PlayMode 优先，最后一次平台构建 | 全量 EditMode/PlayMode、远端 Hash/Range/写入 405、ARM64 APK 审计 | 已完成 |

## 六、UI 方向

视觉关键词：训练家终端、图鉴设备、探索地图、竞技场记分板。

- 主背景使用深海军蓝；功能面板用暖白/浅灰，避免当前页面全部陷在相近深色中。
- 主要动作使用高辨识红色，信息/选中态使用蓝色，稀有或新获得状态使用电光黄。
- 页头加入原创圆形状态灯与细扫描线；标签和按钮使用胶囊轮廓，不放官方精灵球图标。
- 卡图始终是视觉中心，装饰不得遮挡卡牌文字与编号。
- 页面进入、详情翻面、语言版本切换与下载完成都保留短动画和统一音效；开启“减少动态效果”后用静态颜色/描边替代位移和缩放。
- 动画默认以 30 FPS 低成本调度或一次性过渡完成，不为不可见元素保留每帧任务。

## 七、性能预算

- 首屏不读取不可见卡图；所有大型列表继续使用固定高度虚拟化。
- 同一图片请求去重；离开行、切换语言或关闭详情时取消已经失去消费者的请求。
- 卡图解码与 UI 赋值分离；主线程每帧只提交有限数量结果，避免连续解码造成触控卡顿。
- 收藏/图鉴搜索使用短防抖并复用预计算的规范化搜索键，不在每次按键时遍历和分配全部字符串。
- 语言版本组和物种关联在 Catalog 加载时构建只读索引，详情页不得每次扫描全量 Printings。
- 手机缓存以容量和张数双门槛约束；页面关闭后不能因为事件、调度器或图片视图残留而持续增长。

## 八、Git 与验证规则

- 3B、3D、3E、3F、每种语言导入/发布和最终验收分别形成独立提交。
- 只暂存当前切片文件；不包含现有字体、主题 `.meta`、Render Pipeline、ProjectSettings 或 Addressables 用户改动。
- 普通 UI/逻辑变更先执行定向 PlayMode、完整 PlayMode和必要 EditMode；只有最终 Android 平台边界才重建 APK。
- 任何语言数据发布都必须先完成本机 Hash、离线安装回读和完整性审计，再更新公开 Catalog。

## 九、3A–3B 完成记录（2026-07-30）

- 容量基线直接读取正式 APK、`LocalContent` 和 revision 3 Catalog；没有用旧日志代替当前收据。
- `PrintingLanguageIndex` 为每个 Printing 提供稳定版本组；同一 Item/Variant 的不同语言可自动合并，跨地区 Set 可通过已复核显式组关联，未关联的单语言卡保持单例。
- 运行时组拒绝同一语言出现两个候选、跨游戏成员、缺失 Printing 和重复归组；详情选择只接收卡片语言参数，不读取应用语言。
- `LanguageSelectionService` 新增双向隔离回归：切换卡片语言不会触发应用语言事件，切换应用语言也不会改变已保存或已解析的卡片语言。
- 改动前基线 6/6 通过；3B 定向 EditMode 15/15、完整 EditMode 304/304 通过。基线连续抽 1,000 包 / 11,000 张耗时 0.046 秒、托管内存增长 0.035 MiB，后续性能重点据此放在 UI、搜索和图片管线。

## 十、3D 完成记录（2026-07-30）

- 收藏详情根据 `PrintingLanguageIndex` 的实际已安装成员动态建立 `中 / EN / 日` 标签；单语言组清空并隐藏整个切换器，不保留空占位。
- 切换版本会同时更新卡图、卡名、系列、卡号、稀有度、变体、语言徽标、拥有数和 NEW 状态；每个语言 Printing 继续独立记账与已读。
- 详情切换只调用局部 `SwitchDetailCardLanguage`，不调用应用语言或全局卡片语言服务；测试确认两种全局状态和事件均不变化。
- 语言切换播放统一翻卡音效并使用 120 ms 卡图淡入；开启“减少动态效果”后立即切换，不启动调度动画。
- 定向 EditMode 3/3、定向 PlayMode 2/2、完整 EditMode 304/304、完整 PlayMode 9/9 通过。PlayMode fixture 同时覆盖三语言皮卡丘和英文独占训练家卡。

## 十一、3C、3E、3F 完成记录（2026-07-30）

- 私人导入器已完成英文、日文、简中资料抓取、规范化、卡图落盘、稳定 package id、确定性 ZIP、SHA-256 和正式安装器回读；日文来源含 `+` 的 Set id 会安全转义，来源身份与显示资料不变。
- 三语言同名卡以可审计版本组浏览；只有来源实际存在的语言才出现标签，切换时同步更新卡图和印刷资料，单语言卡不显示伪按钮。
- 开始页、主菜单、设置、收藏与图鉴沿用原创训练家终端视觉；按钮、页面、翻卡与下载均保留统一动画和点击音效，“减少动态效果”关闭位移/缩放但不删除状态反馈。
- 列表保持虚拟化，搜索复用规范化索引，图片管线继续使用请求合并、取消与有界缓存；大资料量不会在首屏一次解码全部卡图。

## 十二、语言隔离与最终验收（2026-07-30）

- 首装无本地卡牌 Catalog 的模拟器测试发现：设置页曾把“缺少卡牌语言”误当成整个语言面板不可用。现已修复为只禁用卡片语言选择器，应用语言由已安装 UI Locale 独立决定；缺少内容的状态也改用本地化玩家提示，不再显示电脑路径。
- 隔离政策具有独立自动回归。Android 14 x86_64 验收包在无本地卡牌目录下成功把应用界面切换为中文，同时 PlayerPrefs 保持 `settings.ui-language=zh`、`settings.content-language=en`；卡片语言没有被应用语言改写。
- revision 4 Catalog 共 537 包，Catalog SHA-256 为 `4e01bb463c0c9b6952bb40d6417257da2c095e096932756f1918e6ad33ec9d89`，package identity SHA-256 为 `141c62be2ac30186fcf89c8dd337112e21131f72734021acfa1cc72654d1e24a`。公开 Site 的 HEAD 537/537、Range 537/537、匿名写入拒绝 8/8，且读取未发送授权头。
- 最终自动回归为 EditMode 344/344、PlayMode 10/10。ARM64 APK 为 52,643,378 bytes，SHA-256 `cdf68dd6f9cc2cb796d437ef22a61774a25df650d964bc16f7ed711a1234b2a4`；纯 ARM64、权限边界、签名、zipalign、私人内容 0 命中均通过。
- `project_completion_audit.ps1 -RequireComplete` 已核对本地、公开 HTTPS Site、唯一 Android 目标、安装配置和 14/14 模拟器软件收据，输出 `PROJECT COMPLETION VERIFIED: 100%`。Cloudflare R2 凭据仍是非阻塞后续项，当前只读 Site 已满足本阶段发布范围。
